package PracticeProblems.OOPsPrograms;

/* 14.	Create a base class Shape with a method calculateArea().
    Create subclasses Circle, Rectangle, and Triangle that override the calculateArea() method
    to calculate and return the area of the respective shapes.
   Circle area = pi*r*r
   Rectangle area = width * length
   Triangle area = 0.5 * Base * Height
 */

// created a base class named Shape with a method calculateArea().
public class Shape {

    public double calculateArea(){
        return 0.0;
    }
}
